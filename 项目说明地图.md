# Jow 的网站 · 项目中文地图（一看就懂）

> 作用：这是你这个项目的「说明书」。真实的文件/文件夹都叫英文（网站和部署平台认死这些名字，改了会坏），
> 你**不需要记英文**——看本文件 + 打开同目录的「中文导览」文件夹即可。
>
> 项目新位置：`/Users/zhuanzmima0000/Documents/Jow的网站`

---

## 一、这个文件夹里到底是什么？

它**不是**一个单纯的网站，而是「三件套」打包在一起：

| 编号 | 中文叫法 | 真实英文名 | 一句话说明 |
|------|----------|------------|------------|
| ① | 官网 | `jowjowork.uno` | 你对外展示的个人工作站主页（**现为首页全屏沉浸视频**：摩托隧道背景 + 极简品牌字，纯静态、无后台，后续将大改版） |
| ② | JowOS 套件 | `jowos*` 一系列文件 | 一个「人生仪表盘」小应用 + 让它跨设备（手机/电脑）云同步的工具 |
| ③ | LUUC 独立站 | `LUUC/` | 另一个独立个人网站（博客/相册/留言板），自己有 git 仓库、部署在 Netlify |

> 简单记：**官网是门面，JowOS 是你的随身工具，LUUC 是另一个小站。**

---

## 二、文件/文件夹逐个说明（中文理解 ← 真实英文名）

### A. 官网本体（jowjowork.uno）—— 你 90% 的维护都在这里

| 中文理解 | 真实文件/夹 | 干什么的 | 你平时动不动 |
|----------|-------------|----------|--------------|
| 网站首页（全屏视频页） | `index.html` | 整个官网**首页**：背景铺满摩托隧道视频（自动播放/静音/循环），顶部居中导航（AI经验分享 / Skills分享 / 商业合作），中央品牌 + slogan，底部社媒 + 版权。改首页文案、导航或换视频改这里。 | 常改 |
| 网站样式表（外观） | `css/`（里面 `style.css`） | 全部视觉效果：配色、玻璃质感、辉光、手机适配。改外观才动。 | 偶尔 |
| 网站交互脚本 | `js/`（里面 `main.js`） | 交互动画：导航玻璃态、滚动揭示、作品卡 3D 倾斜、进度条。改交互才动。 | 偶尔 |
| 域名绑定文件 | `CNAME` | 固定写着 `jowjowork.uno`（告诉 GitHub Pages 这是你的域名）。 | 别动 |
| 关闭旧引擎标记 | `.nojekyll` | 告诉 GitHub Pages 别用 Jekyll 处理（空文件，有它就够）。 | 别动 |
| 自动部署配置 | `.github/`（里面 `workflows/deploy.yml`） | 你一推送代码，它就自动把网站发到线上。一般不用动。 | 别动 |
| 忽略清单 | `.gitignore` | 告诉 git 哪些文件不上传（如缓存、临时文件）。 | 别动 |

### A-2. 首页视频与旧版备份（新增于 2026-08-10）

| 中文理解 | 真实文件/夹 | 干什么的 | 你平时动不动 |
|----------|-------------|----------|--------------|
| 首页背景视频 | `media/`（里面 `hero.mp4`） | 首页全屏循环播放的摩托车隧道视频（约 5 秒、1080p、19MB、H.264）。**要换首页视频，就替换这个 `hero.mp4` 文件**。 | 偶尔 |
| 旧官网备份 | `index-旧版备份.html` | 改版前那版 decart 风格官网的完整备份，万一想恢复旧版用。 | 别动 |

### A-3. 顶部导航占位页（新增于 2026-08-10）

首页顶部现在有三个居中导航入口，目前都先跳转到占位页，你可以后续慢慢填内容。

| 中文理解 | 真实文件/夹 | 干什么的 | 你平时动不动 |
|----------|-------------|----------|--------------|
| AI经验分享页 | `ai-sharing.html` | 顶部导航第一个入口，未来放 AI 工具/工作流经验文章。现在是占位页。 | 后续常改 |
| Skills分享页 | `skills.html` | 顶部导航第二个入口，未来放可下载的 Skills 技能包。现在是占位页。 | 后续常改 |
| 商业合作页 | `services.html` | 顶部导航第三个入口，未来写清楚你提供什么服务、怎么合作。现在是占位页。 | 后续常改 |

### B. JowOS 套件（人生仪表盘 + 跨设备同步）

| 中文理解 | 真实文件/夹 | 干什么的 | 你平时动不动 |
|----------|-------------|----------|--------------|
| JowOS 应用本身 | `jowos/`（里面 `index.html`） | 那个「人生仪表盘」网页应用，部署在 `jowjowork.uno/jowos/`，数据默认存在浏览器本地。 | 偶尔 |
| 自托管同步服务器 | `jowos-sync-server.py` | 一个 Python 小程序：把一台**一直开机的安卓手机**当「共享硬盘」，让 Mac/手机数据互通。 | 部署时动 |
| 云端同步 Worker | `jowos-worker.js` | 另一套同步方案：跑在 Cloudflare 上，读写你的 GitHub 私有 Gist（无需自己服务器）。 | 部署时动 |
| 手机一键部署脚本 | `jowos_setup.sh` | 在手机 Termux 里运行，自动装 Python、放好服务器、启动服务。 | 部署时动 |
| 手机开机自启脚本 | `termux-boot.sh` | 手机重启后自动拉起同步服务器（需装 Termux:Boot）。 | 部署时动 |
| 云端同步部署指南 | `JowOS云同步部署指南.md` | 图文教你怎么用「Cloudflare Worker + 私有 Gist」那套方案。 | 照着做 |
| 手机同步部署步骤 | `手机同步服务器部署步骤.md` | 图文教你怎么用「安卓手机当服务器」那套方案。 | 照着做 |

> 两套同步方案二选一即可：**云端版（Cloudflare）** 省事、**手机自托管版** 数据不出家门更私密。

### C. LUUC 独立个人站

| 中文理解 | 真实文件/夹 | 干什么的 | 你平时动不动 |
|----------|-------------|----------|--------------|
| LUUC 小站 | `LUUC/` | 一个**独立**的极简个人网站（首页/博客/作品集/画廊/留言板），用 Netlify 托管。**它自己带一个 git 仓库**（里面有 `LUUC/.git`），目前还没并入官网仓库。 | 另管 |

> 注意：`LUUC/` 是嵌套的独立项目，改它要走它自己的 git/Netlify 流程，别和官网搞混。

### D. 仓库本身（不用管，知道就行）

| 中文理解 | 真实文件/夹 | 说明 |
|----------|-------------|------|
| 官网 git 仓库 | `.git/`（隐藏） | 官网的版本历史 + 远程地址 `github.com/LeonJow/jowjowork.uno`。你改完代码 `git push` 就上线。 |
| LUUC 的 git 仓库 | `LUUC/.git/`（隐藏） | LUUC 自己的版本历史，和官网仓库互不相干。 |

---

## 三、怎么用「中文导览」文件夹（重点）

同目录下有个 **「中文导览」** 文件夹，里面每个「中文名」都是一个**软链接（快捷方式）**，点进去直接打开真实的英文文件。
你以后想找某个文件是干嘛的，**直接进「中文导览」看中文名点开就行**，不用面对一堆英文。

例如：
- 想改官网文字 → 打开 `中文导览 / 网站首页（index.html）`
- 想改外观 → 打开 `中文导览 / 网站样式表（css）`
- 想看 JowOS 说明 → 打开 `中文导览 / JowOS 云同步部署指南（.md）`

> 「中文导览」只是给你看的导航，**不参与网站运行、也不会被发布到线上**（已写进 `.gitignore`）。

---

## 四、日常怎么维护（速查）

**1. 改内容（最常见）**：编辑 `index.html` 里对应的文字，保存即可。
**2. 本地预览**：在 `Jow的网站` 目录里运行
```bash
python3 -m http.server 8000
```
浏览器打开 `http://localhost:8000` 看效果。
**3. 发布上线**：改完确认无误后
```bash
git add -A
git commit -m "更新：xxx"
git push origin main
```
约 1 分钟后访问 https://jowjowork.uno 生效（GitHub Actions 自动部署，无构建步骤）。

---

## 五、安全提醒（建议你处理）

**同步口令已修复（2026-08-10）**：仓库内不再硬编码任何真实口令。
- `jowos_setup.sh` 改为「优先读环境变量 `JOWOS_KEY`，未设置则交互式输入，为空则中止」，不再写死明文。
- `termux-boot.sh` 不再 export 口令，服务端自动从同目录 `jowos-key.txt` 读取。
- 新口令请在「本地/手机/Cloudflare Worker 环境变量」三处使用同一个值，且**不要提交进 git**。
- ⚠️ 提醒：若旧口令此前已 push 到公开 GitHub，它仍留在 git 历史中无法彻底抹除；换新口令即可让旧值失效，今后切勿再把真实口令写进会被提交的脚本。

---

> 本地图由 WorkBuddy 于 2026-08-10 生成，对应项目从 `Documents/20-开发/代码项目/GitHub/jowjowork.uno` 迁移至 `Documents/Jow的网站`。
