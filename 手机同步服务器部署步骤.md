# JowOS 自托管同步服务器（荣耀 X30i / Termux）

把一直插电、一直开机的荣耀 X30i 当「共同数据存储空间」，让 Mac / iPhone / 安卓本机都跨设备同步。
数据只在这台手机上，不出家门，最贴「本地最强」。

## 前置
- 手机：Termux 已装；Python 已装（`pkg install python`）。
- 手机：装 **Termux:Boot**（F-Droid 上，用于开机自启服务）。
- Mac：已 `adb` 连上手机（用户已有 ADB 权限）。
- 路由器：给手机保留固定 LAN IP（仅在家同步就够）；要出门同步再装 Tailscale。

## 步骤（手机连上 Mac 后由 WorkBuddy 协助执行）
1. 确认环境：`adb shell` 进手机 → `termux` 里 `python3 --version`、`pkg list-installed | grep python`。
2. 把 `jowos-sync-server.py` 推到手机 Termux 家目录：
   `adb push jowos-sync-server.py /data/data/com.termux/files/home/`
3. 生成口令：Mac 上 `openssl rand -hex 24` 得到一长串，记下来。
4. 启动测试（Termux 里）：
   ```
   export JOWOS_KEY="上面那串"
   python3 ~/jowos-sync-server.py --host 0.0.0.0 --port 8765 --data ~/jowos-data.json
   ```
5. 从 Mac 测连通（手机 LAN IP 如 192.168.x.x）：
   `curl -H "x-jowos-key: 那串" http://192.168.x.x:8765/` 应返回 `{}`。
6. 设开机自启：把 `termux-boot.sh` 改名放 `~/.termux/boot/` 并 `chmod +x`，里面填同一个 `JOWOS_KEY`。
7. 路由器给手机保留固定 IP（或装 Tailscale，手机和其他设备都加进去）。
8. JowOS 页面「同步」卡片：启用 → 填 `http://192.168.x.x:8765`（家）/ Tailscale 地址（出门）→ 填同一个口令 → 立即同步。

## 验证跨设备
- Mac 加一条任务 → 约 1–2 秒推到手机。
- 手机浏览器（同 Wi‑Fi）打开 JowOS → 看到这条任务。

## 注意
- 安卓可能杀 Termux 后台：靠 Termux:Boot + `termux-wake-lock` 缓解；插电时不激进。
- 单点风险：手机重置/坏 -> 数据没了。定期「仅推送」或导出 JSON 备份到 Mac。
- 出门同步必须 Tailscale（或路由器端口映射，较不安全），否则只能在家 LAN 用。
